{
  "name": "twillingate-sdk",
  "version": "2.0.0",
  "private": true,
  "description": "Source of the embedded js/twillingate.js SDK. `npm run build` writes ../internal/server/twillingate.js, which is committed and served by the collector.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.25.9",
    "jsdom": "26.1.0",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  }
}
